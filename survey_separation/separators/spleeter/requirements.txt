# Install in a SEPARATE virtualenv from the harness (TensorFlow vs torch).
#   python -m venv .venv-spleeter && .venv-spleeter/Scripts/pip install -r requirements.txt
# then pass --spleeter-python .venv-spleeter/Scripts/python
spleeter
tensorflow
